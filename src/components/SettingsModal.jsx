import { View, Text, Modal } from "react-native";

const SettingsModal = ({}) => {
	return (
		<Modal>
			<View>
				<Text>Settings Modal</Text>
			</View>
		</Modal>
	);
};

export default SettingsModal;
